

let accessToken;


Spotify = {
  getAccessToken(){
    // check if token is there
    if(accessToken){
      return accessToken;
    }
    //check url to see if token has just been obtained
    
  }
}



export default Spotify
